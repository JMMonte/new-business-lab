---
description: Make the build/pivot/kill call and write the decision memo + pitch (Stage 7)
argument-hint: "<business slug>"
---

Make the decision for `businesses/$1/` (follow
`${CLAUDE_PLUGIN_ROOT}/frameworks/07-decide.md`).

Pull together the problem brief, audit, simulation, and any test results. Check the
**kill criteria written back in the audit** — were any hit? Run the **Simplicity
Filter** (the five plain-language questions; or invoke `/simplify` first), with the
load-bearing one being *"why would anyone pay for this instead of just buying/using
what exists?"*.

Write two files:
- `businesses/$1/07-decision-memo.md` — **BUILD / PIVOT / KILL** with the reasoning,
  what was believed vs measured, and (if build) the first milestone + next
  assumption to de-risk.
- `businesses/$1/pitch.md` — the simplicity-filter answers, in plain language.

Lead with the verdict and the one-breath description. A KILL (including "fails the
simplicity filter") is a success — it saved years. Be honest; round honestly; cut
jargon.
